export const account = {}; // No longer needed in React
export const savingsPot = {}; // No longer needed in React
export const transactions = []; // No longer needed in React
export const payees = []; // No longer needed in React
export const showAddPayee = false; // No longer needed in React
export const showPayPayee = false; // No longer needed in React